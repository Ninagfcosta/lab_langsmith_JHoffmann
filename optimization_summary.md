# Optimization Summary

> Fill this in after running `src/cost_performance.py` and comparing the two experiments in LangSmith. Replace the [brackets] with your real numbers.

Comparing `gpt-4o-mini` and `gpt-4o` on the same 15-example dataset and evaluators, `gpt-4o-mini` scored **[X.XX]** on correctness and **[X.XX]** on clarity at an estimated cost of **[$0.00XX]** per example, while `gpt-4o` scored **[X.XX]** / **[X.XX]** at roughly **[N]x** the cost per example; my recommended trade-off is **[e.g. "gpt-4o-mini for this tutoring use case, since the correctness gap was small (<0.X) but the cost difference was over Nx"]** — I would pick **gpt-4o-mini** when budget or latency matters more than squeezing out the last bit of accuracy, and **gpt-4o** only for the hardest questions (e.g. "hard" difficulty category) if correctness on those specifically justifies the extra cost.

## Comparison table

| Model | Avg. correctness | Avg. clarity | Est. cost / example | Notes |
|---|---|---|---|---|
| gpt-4o-mini | [ ] | [ ] | [ ] | |
| gpt-4o | [ ] | [ ] | [ ] | |

## When to pick each

- **Best for cost efficiency:** [ ]
- **Best for maximum performance:** [ ]
- **Best balanced option:** [ ]
